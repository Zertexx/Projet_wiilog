{% extends 'base.html.twig' %}

{% block title %}Edit Enterprise{% endblock %}

{% block body %}

    <h1 class=" jumbotron p-3">Edition de l'entreprise : {{ enterprise.name }}</h1>

    {{ include('enterprise/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary" href="{{ path('enterprise_index') }}">back to list</a>

    {{ include('enterprise/_delete_form.html.twig') }}
{% endblock %}
